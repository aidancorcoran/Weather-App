// Define a function to process the data
function processData(data) {
    const apiKey = toString(data)
    return(apiKey)
}
  
// Export the function so it can be used in main.js
module.exports = {
    processData
};
  